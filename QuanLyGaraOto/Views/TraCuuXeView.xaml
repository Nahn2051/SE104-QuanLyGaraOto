<UserControl x:Class="QuanLyGaraOto.Views.TraCuuXeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <Color x:Key="PrimaryColor">#1565C0</Color>
        <Color x:Key="SurfaceColor">#FFFFFF</Color>
        <Color x:Key="BackgroundColor">#F5F7FA</Color>
        <Color x:Key="TextPrimaryColor">#212121</Color>
        <Color x:Key="BorderColor">#E0E0E0</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    </UserControl.Resources>

    <Border Background="{StaticResource BackgroundBrush}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- KHUNG TÌM KIẾM -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="10" ShadowDepth="1" Opacity="0.05" Direction="270" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="🔍 TRA CỨU XE" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,16" />
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Ô nhập tìm kiếm, cập nhật ngay khi gõ bằng UpdateSourceTrigger=PropertyChanged -->
                        <TextBox Grid.Column="0" Text="{Binding TuKhoa, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="44" Padding="12,0" FontSize="15" 
                                 VerticalContentAlignment="Center"
                                 BorderBrush="{StaticResource BorderBrush}" BorderThickness="1.5"
                                 ToolTip="Nhập Biển số, Tên chủ xe hoặc Hiệu xe" />
                        
                        <Button Grid.Column="2" Content="Tìm kiếm" Command="{Binding TimKiemCommand}" 
                                Height="44" Padding="24,0" FontSize="15" FontWeight="Bold" 
                                Foreground="White" Background="{StaticResource PrimaryBrush}" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.9" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                                </Setter.Value>
                                            </Setter>
                                            <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                    <TextBlock Text="Mẹo: Hệ thống tự động tìm theo biển số, tên chủ xe và hiệu xe. Gõ chữ thường/hoa đều được." 
                               FontSize="12" Foreground="#9E9E9E" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <!-- DANH SÁCH KẾT QUẢ -->
            <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="10" ShadowDepth="1" Opacity="0.05" Direction="270" />
                </Border.Effect>
                
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Kết quả tra cứu" FontSize="15" FontWeight="SemiBold" Foreground="#757575" Margin="0,0,0,12" />

                    <DataGrid ItemsSource="{Binding DanhSachXe}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#ECEFF1"
                              BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                              RowHeight="42" ColumnHeaderHeight="42"
                              Background="White" RowBackground="White" AlternatingRowBackground="#FAFBFC">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F7FA" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Padding" Value="10,0" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,0" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD" />
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Biển số" Binding="{Binding BienSo}" Width="*" />
                            
                            <!-- Bind theo Navigation Property HieuXe.TenHieuXe -->
                            <DataGridTextColumn Header="Hiệu xe" Binding="{Binding HieuXe.TenHieuXe}" Width="*" />
                            
                            <DataGridTextColumn Header="Tên chủ xe" Binding="{Binding TenChuXe}" Width="1.5*" />
                            
                            <!-- Cột Tiền nợ được Format tiền tệ đẹp -->
                            <DataGridTextColumn Header="Tiền nợ" Binding="{Binding TienNo, StringFormat=N0}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
