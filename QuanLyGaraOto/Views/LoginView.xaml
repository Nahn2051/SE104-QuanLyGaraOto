<Window x:Class="QuanLyGaraOto.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Đăng Nhập — Quản Lý Gara Oto"
        Height="580" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- ============================================================= -->
        <!-- COLORS                                                         -->
        <!-- ============================================================= -->
        <Color x:Key="PrimaryColor">#1565C0</Color>
        <Color x:Key="PrimaryDarkColor">#0D47A1</Color>
        <Color x:Key="AccentColor">#26A69A</Color>
        <Color x:Key="SurfaceColor">#FFFFFF</Color>
        <Color x:Key="BackgroundStart">#667eea</Color>
        <Color x:Key="BackgroundEnd">#764ba2</Color>
        <Color x:Key="TextPrimaryColor">#212121</Color>
        <Color x:Key="TextSecondaryColor">#757575</Color>
        <Color x:Key="PlaceholderColor">#BDBDBD</Color>
        <Color x:Key="BorderColor">#E8EAF0</Color>
        <Color x:Key="ErrorColor">#E53935</Color>
        <Color x:Key="ErrorBgColor">#FFEBEE</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
        <SolidColorBrush x:Key="PlaceholderBrush" Color="{StaticResource PlaceholderColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />

        <!-- Bool → Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- ============================================================= -->
        <!-- INPUT TEXTBOX STYLE                                            -->
        <!-- ============================================================= -->
        <Style x:Key="LoginTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="48" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Background" Value="#F8F9FA" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                <Setter TargetName="border" Property="BorderThickness" Value="2" />
                                <Setter TargetName="border" Property="Background" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ============================================================= -->
        <!-- LOGIN BUTTON STYLE                                             -->
        <!-- ============================================================= -->
        <Style x:Key="LoginButton" TargetType="Button">
            <Setter Property="Height" Value="50" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="12" SnapsToDevicePixels="True">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#667eea" Offset="0" />
                                    <GradientStop Color="#764ba2" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#667eea" BlurRadius="16"
                                                  ShadowDepth="3" Opacity="0.35" Direction="270" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667eea" BlurRadius="24"
                                                          ShadowDepth="5" Opacity="0.5" Direction="270" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#BDBDBD" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ============================================================= -->
        <!-- CLOSE BUTTON (X) STYLE                                         -->
        <!-- ============================================================= -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="#9E9E9E" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="18"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5" />
                                <Setter Property="Foreground" Value="#E53935" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ================================================================= -->
    <!-- MAIN CARD                                                          -->
    <!-- ================================================================= -->
    <Border CornerRadius="20" Background="White"
            Margin="10"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="40"
                              ShadowDepth="4" Opacity="0.15" Direction="270" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <!-- Row 0: Header gradient + Close btn -->
                <RowDefinition Height="170" />
                <!-- Row 1: Form content -->
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- ========================================================= -->
            <!-- HEADER — Gradient banner + Icon                            -->
            <!-- ========================================================= -->
            <Border Grid.Row="0" CornerRadius="20,20,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#667eea" Offset="0" />
                        <GradientStop Color="#764ba2" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <!-- Close button -->
                    <Button Style="{StaticResource CloseButton}"
                            Content="✕" FontWeight="Bold"
                            Foreground="White" Opacity="0.7"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Margin="0,12,12,0"
                            Click="CloseButton_Click" />

                    <!-- Logo & Title -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <!-- Car Icon -->
                        <Border Width="64" Height="64"
                                CornerRadius="32"
                                Background="#FFFFFF" Opacity="0.9"
                                HorizontalAlignment="Center">
                            <TextBlock Text="🚗" FontSize="30"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <TextBlock Text="GARA ÔTÔ" FontSize="22" FontWeight="Bold"
                                   Foreground="White" Margin="0,12,0,0"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Hệ thống quản lý gara ô tô" FontSize="12"
                                   Foreground="White" Opacity="0.8" Margin="0,4,0,0"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ========================================================= -->
            <!-- FORM CONTENT                                               -->
            <!-- ========================================================= -->
            <StackPanel Grid.Row="1" Margin="36,30,36,32">

                <TextBlock Text="Đăng nhập" FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Vui lòng nhập tài khoản để tiếp tục"
                           FontSize="12.5" Margin="0,6,0,24"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" />

                <!-- ───── Error Message ───── -->
                <Border CornerRadius="10" Padding="14,10" Margin="0,0,0,16"
                        Visibility="{Binding IsErrorVisible, Converter={StaticResource BoolToVis}}">
                    <Border.Background>
                        <SolidColorBrush Color="{StaticResource ErrorBgColor}" />
                    </Border.Background>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠" FontSize="14" Margin="0,0,8,0"
                                   Foreground="{StaticResource ErrorBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding ErrorMessage}" FontSize="13"
                                   Foreground="{StaticResource ErrorBrush}"
                                   VerticalAlignment="Center" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <!-- ───── Username ───── -->
                <TextBlock Text="Tên đăng nhập" FontSize="13" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           Margin="4,0,0,8" />
                <TextBox x:Name="UsernameBox"
                         Style="{StaticResource LoginTextBox}"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,18" />

                <!-- ───── Password ───── -->
                <TextBlock Text="Mật khẩu" FontSize="13" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           Margin="4,0,0,8" />
                <PasswordBox x:Name="PasswordBox"
                             Height="48" Padding="16,12" FontSize="14"
                             Margin="0,0,0,28"
                             PasswordChanged="PasswordBox_PasswordChanged">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12" />
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>

                <!-- ───── Login Button ───── -->
                <Button Content="Đăng Nhập"
                        Style="{StaticResource LoginButton}"
                        Command="{Binding LoginCommand}" />
            </StackPanel>

        </Grid>
    </Border>
</Window>
